
"""
Các hàm xử lý chuỗi quan trọng nhất trong Python bao gồm:
    Chuyển toàn bộ chuỗi thành chữ hoa: upper() 
    Chuyển toàn bộ chuỗi thành chữ thường: lower() 
    Viết hoa chữ cái đầu mỗi từ: title() 
    Viết hoa chữ cái đầu tiên của chuỗi: capitalize()
    Tìm vị trí đầu tiên của chuỗi con sub: find()
    Thay thế chuỗi con: replace()
    Tách chuỗi thành danh sách dựa vào ký tự phân tách: split()
    Nối danh sách thành chuỗi: join()
    Xóa khoảng trắng đầu và cuối chuỗi: strip()
    Kiểm tra chuỗi bắt đầu bằng ký tự con: startswith()
    Kiểm tra chuỗi kết thúc bằng ký tự con: endswith()
    Kiểm tra chuỗi chỉ gồm chữ cái: isalpha()
    Kiểm tra chuỗi chỉ gồm chữ số: isdigit()
    Kiểm tra chuỗi gồm chữ cái và số: isalnum()
    Kiểm tra chuỗi chỉ gồm khoảng trắng: isspace()
"""
